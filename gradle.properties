org.gradle.caching = true
org.gradle.jvmargs = -Xms512M -Xmx2048M
org.gradle.parallel = true
android.useAndroidX = true
kotlin.code.style = official
version = 1.0.0
# GitHub Packages credentials are expected to be provided via
# gradle.properties in your local Gradle user home (~/.gradle/gradle.properties)
# or via environment variables (GITHUB_ACTOR / GITHUB_TOKEN).
# Do NOT commit real tokens here.
gpr.user =
gpr.key =
